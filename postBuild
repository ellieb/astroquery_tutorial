python2.7 -m pip install gvar --no-deps
python2.7 -m pip install lsqfit --no-deps
python2.7 -m pip install pp --no-deps
python2.7 -m pip install bottleneck
python2.7 -m pip install https://github.com/thomasorb/orb/archive/master.zip
python2.7 -m pip install https://github.com/thomasorb/orcs/archive/master.zip

jupyter contrib nbextension install --user
jupyter nbextension enable --py widgetsnbextension
jupyter nbextension enable --py --sys-prefix ipyaladin

# Notebooks w/ extensions that auto-run code must be "trusted" to work the first time
jupyter trust astroquery_example_kbos.ipynb
